public class onesixnine {
   /* ArrayList<Integer> b = new ArrayList<Integer>();
    for(int i=0;i<N;i++)
    {
        Node temp = a[i];
        while(temp!=null)
        {
            b.add(temp.data);
            temp = temp.next;
        }
    }
    Collections.sort(b);
    Node head = new Node(b.get(0));
    Node temp = head;
    for(int i=1;i<b.size();i++)
    {
        temp.next  = new Node(b.get(i));
        temp = temp.next;
    }
   return head;
   }*/
}
